# QuickDaily QA Policy

## 目标

保持 `beta` 分支适合日常开发和 Codex 检索；完整设备证据用于正式版验收，但不作为每个测试版本的长期 Git 内容。

## beta：增量验收

- 只验收本次新增功能，以及被直接影响的回归路径。
- 可测的确定性逻辑先补单元测试；UI 变化只保留一条代表性成功路径，必要时再补一条失败或权限路径。
- 只提交简短结果摘要；不要提交逐步 UI XML、重复截图、完整 logcat、package dump、ADB 命令转录或 Perfetto 原始文件。
- 日志、Perfetto 和完整截图保存在本地 `qa-evidence/<version>/`，仅在失败、性能专项或需要复核时保留。

## main：正式版验收

- 正式版候选冻结后，执行完整验收矩阵，覆盖编辑器、设置、任务/日记小组件、快捷入口、悬浮窗、附件和 Wikilink 等受影响功能。
- 正式版证据可以完整保存，但原始文件放在仓库外；Git 中只保留版本、设备、测试范围、结果和问题链接等摘要。

## 每次验收的最小记录

- 版本号、提交号、设备/API、验收范围、结果和未覆盖项。
- 失败时附最短复现步骤和外部原始证据路径；成功时不保留逐步骤原始转储。
- 提交前运行 `git diff --check`，并按变更范围运行单元测试和构建门禁。

## 分支约定

- `beta` 是增量验收分支。
- `main` 是正式版分支；完整验收完成后再合并或更新。
