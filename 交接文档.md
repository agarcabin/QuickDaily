# QuickDaily 1.6 交接文档

> 更新日期：2026-07-19
> 当前版本：`1.6`（`versionCode 53`）
> 发布方式：本地 Release APK、GitHub tag 与 GitHub Release

## 1. 本轮完成的功能

### 小部件可靠性与数据读取

- 新增 `WidgetRefreshCoordinator`：统一协调阅读/任务小部件刷新，自动刷新合并 400ms，同类刷新最少间隔 2 秒。
- `TaskWidgetService` 与 `QuickDailyReadWidgetService` 对同类小部件读取加入共享锁，防止多个实例同时扫描相同的日记文件。
- 阅读小部件状态已区分：初始为 `Loading...`；日记不存在或内容为空为 `暂无日记`；路径错误或读取异常为 `无法读取日记`。
- 任务小部件支持今天、最近 7 天、最近 30 天；任务项带来源日期和位置，可修改非当天任务为 `- [X]`。
- 任务刷新成功后会在小部件标题短暂显示“刷新成功”。MIUI 会禁止后台 BroadcastReceiver 的 Toast，不要恢复为仅 Toast 提示。
- 小部件默认背景保留 16dp 圆角。Android 12 以上支持自定义背景色/透明度；Android 8–11 使用黑/白圆角资源回退，避免调用不兼容的 `RemoteViews.setColorStateList`。

### 编辑器、标签与附件

- 首页编辑器：输入单独 `#` 可显示最近 3 个标签；已有标签会从 Obsidian 库扫描补齐；标签渲染为蓝色。
- 悬浮速记窗：标签候选改为工具栏上方横向列表，回车有候选时优先选择第一项，无候选时才保存。
- 悬浮窗附件使用 `OpenDocument`，保存时复制到配置的附件目录，并写入 `![[相对路径/原文件名.扩展名]]`。
- **附件复制失败时必须中止保存并告知用户**。不要把 `content://` URI 当作最终 Markdown 链接写入日记。
- 图片选择后使用内置正方形裁剪器；裁剪矩阵已限制为始终覆盖裁剪框，不能删除该边界限制，否则会生成黑边。

### 设置与系统入口

- “设置 → 小部件”新增统一外观：白底黑字、黑底白字、自定义颜色和透明度。
- 外观 Slider 在拖动时只更新界面草稿，**仅在松手时**调用 `saveConfig` 和刷新小部件。不要在 `onValueChange` 中持久化或立即刷新，否则会重新引入三星/华为桌面卡顿。
- 系统小部件名称：`任务小部件`、`便签小部件`。
- 自定义图片裁剪后会同步更新快速添加小部件和快捷方式图标。
- 小米/Redmi/POCO 不走 `ShortcutManager.requestPinShortcut`，使用兼容广播路径，避免 MIUI 权限窗口闪退；该路径只能确认“已发送创建请求”，不能伪称创建成功。

## 2. 关键文件与职责

| 文件 | 维护要点 |
|---|---|
| `AppState.kt` | `DiaryConfig`、SharedPreferences 的加载/保存。新增配置字段必须同时经过数据类、读取、`saveConfig` 与 UI。 |
| `WidgetRefreshCoordinator.kt` | 自动刷新协调器。除明确的用户手动操作外，不要绕过其限频。 |
| `QuickDailyReadWidget.kt` / `QuickDailyReadWidgetService.kt` | 便签小部件。源码模式的 `ReadLine.renderInlineMarkdown=false` 必须保留，保证 `#标签` 不被蓝色渲染。 |
| `TaskWidget.kt` / `TaskWidgetService.kt` | 任务周期、跨日任务勾选、刷新反馈与单飞读取。任务定位依赖日期 + 未完成任务序号，不要改回仅按任务文本匹配。 |
| `WidgetAppearance.kt` | 小部件圆角、颜色和低版本兼容。不要直接 `setBackgroundColor`，会丢失圆角。 |
| `WidgetImageCropActivity.kt` | 内置正方形裁剪。缩放/平移后都要调用边界约束。 |
| `NoteEditActivity.kt` | 悬浮速记窗、附件保存、候选标签与回车逻辑。 |
| `EditorScreen.kt` | 首页编辑器、标签补全、蓝色标签渲染、附件工具栏。 |
| `SettingsScreen.kt` | 设置页、外观草稿提交、关于页更新日志。 |

## 3. 容易回归的点

1. **不要在 `MainActivity.onUserLeaveHint()` 中调用 `finishAffinity()`。** 文档选择器或内置裁剪器启动时，部分 ROM 会触发此回调；结束任务栈会让裁剪看起来像闪退。
2. **不要依赖小部件后台 Toast。** 小米系统会记录 `Suppressing toast from package ... by user request`。对桌面操作应使用组件内反馈或前台界面反馈。
3. **不要让 Slider 每一帧刷新小部件。** 配置保存会触发 `loadToday()`，多次 `commit()`、读盘和 RemoteViews 更新会堆积。
4. **RemoteViews 服务是每个 widget 实例各建一份。** 保留服务层共享读取锁；新增耗时读取时也必须纳入该锁或缓存层。
5. **小部件圆角依赖 shape 背景。** 直接设置纯色背景会再次丢失圆角。
6. **源码模式必须保持原始 Markdown。** 新增内联渲染时，确认 `renderInlineMarkdown` 为 false 的行不调用渲染函数。
7. **附件链接要使用 vault 相对路径。** 路径为空、无权限或复制失败时不能继续保存日记。

## 4. 变更后自测清单

- 首页与悬浮窗：单独输入 `#`、选择标签、回车选择第一候选、无候选回车保存。
- 悬浮窗：图片、任意附件、取消附件选择、附件目录无写入权限。
- 便签小部件：渲染/源码切换；源码模式标签不蓝；空日记、无文件、无库路径、读取异常状态。
- 任务小部件：今天/周/月、跨日勾选、重复任务文本、右上刷新反馈、连续快速刷新。
- 小部件外观：白/黑/自定义、透明度滑动、多个同类小部件、Android 8–11 回退外观。
- 裁剪：大图、缩放、拖动到边缘、确认、取消、选择器返回。

## 5. 发布流程

1. 更新 `app/build.gradle.kts` 的 `versionName` 和递增的 `versionCode`。
2. 更新 `version.json` 的版本与发布说明；“关于”页的更新日志位于 `SettingsScreen.kt`。
3. 运行 `gradlew.bat :app:assembleDebug`，再运行 `gradlew.bat :app:assembleRelease`。
4. Release APK 来源：`app/build/outputs/apk/release/app-release.apk`。
5. 本地交付复制到：`C:\Download\互传\QuickDaily-<version>.apk`。
6. GitHub 发布前执行 `git diff --check`、确认没有临时脚本/日志/APK/缓存，再提交、推送 tag，并以同一 APK 创建 Release。
