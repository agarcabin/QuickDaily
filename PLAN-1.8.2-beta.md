# Plan: QuickDaily 1.8.2-beta
_Locked via grill — by Codex + user_

## Goal
完成悬浮窗文案、便签小部件子任务渲染、双小部件完成时间戳处理和 1.8.1 功能日志细化；版本更新为 `1.8.2-beta`，构建并交付本地 APK，不进行 GitHub 发布。

## Approach
1. 将悬浮窗倒三角弹窗中的“今日日记”改为“日记”，保留标题和默认回退逻辑。
2. 让便签小部件仅在 Markdown 渲染模式下复用任务小部件的父子任务规则，携带真实路径、正文行号和原始行，并让 API 31+ 直接集合与旧版服务路径共享数据。
3. 抽取共享任务切换逻辑：勾选时按开关追加 `✅️YYYY-MM-DD`，取消时无条件移除兼容变体的末尾时间戳，保留任务正文；用路径、行号和原始行避免过期点击写错文件。
4. 细化 Beta 日志，覆盖配置快照、悬浮窗/小部件自定义页面选择、读取解析渲染、任务原始行与时间戳动作、写入结果及异常堆栈；日志默认关闭，不记录整篇日记正文。
5. 同步版本元数据为 `1.8.2-beta`，递增 versionCode；运行单元测试、lint、Release 构建、ADB/UI 验收并复制本地 APK。

## Key decisions & tradeoffs
- 便签小部件关闭 Markdown 渲染时仍按纯文本显示；只有 Markdown 模式提供任务层级和交互。
- 两个小部件统一追加/移除完成时间戳；取消时兼容带空格、无空格及既有勾选符号变体。
- 只移除末尾完成时间戳，不改动其前面的任务内容、缩进或其他文本。
- 共享任务写入逻辑保留任务小部件已有的成功后刷新和完成音效行为。
- 保留现有自定义页面持久化、历史记录和 Obsidian 默认配置回退；不做 Git/GitHub 发布。

## Risks / open questions
- ADB 设备需要在验收时保持可用；若设备短暂不可见，构建结果仍需单独报告，设备验证不得伪造。
- 当前工作树存在旧版未跟踪计划、验收资料和截图，必须保留，不覆盖或清理。

## Out of scope
- 不记录整篇日记正文。
- 不改变快速添加小部件、已有自定义页面存储语义或无关编辑器功能。
- 不提交、打标签、推送、创建 Release 或 Pull Request。
