# QD 1.4 — 完整需求规格

---

## 需求一：过滤 Frontmatter 显示

### 目标
在设置中添加开关。开启后，App 内的编辑器、预览模式、桌面便签均隐藏 YAML frontmatter。保存时 frontmatter 完整保留。

### 具体改动

#### 1.1 数据层（AppState.kt）

| 新增字段 | 类型 | 说明 |
|---|---|---|
| `_frontmatter` | `MutableStateFlow<String>` | 存储原始 frontmatter 正文（不含 `---` 分隔符） |
| `filterFrontmatter` | `DiaryConfig` 属性 | 设置开关，持久化到 SharedPreferences，默认 `false` |

**`loadToday()` 的变化**：
- 读取文件完整内容到 `_rawContent`
- 如果 `filterFrontmatter=true` 且内容以 `---\n` 开头：
  - 解析第一个 `---\n...\n---` 之间的文本 → 存入 `_frontmatter`
  - `---` 之后的正文 → 存入 `_diaryContent`（编辑器显示的）
  - 如果找不到闭合 `---`，视作无 frontmatter，全部存入 `_diaryContent`，清空 `_frontmatter`
- 如果 `filterFrontmatter=false` 或无 frontmatter：
  - `_diaryContent` = 完整内容
  - 清空 `_frontmatter`
- 模板加载同理：如果模板含 frontmatter，一样拆开存储，显示时只显示 body

**`onContentChanged()` 的变化**：
- 只更新 `_diaryContent` 为编辑器传来的新文本
- `_frontmatter` 不变

**`saveNow()` 的变化**：
- 如果 `filterFrontmatter=true` 且 `_frontmatter` 非空：
  - 重组 = `---\n${_frontmatter}\n---\n${_diaryContent}`
  - 写入磁盘
- 否则：直接写 `_diaryContent`

#### 1.2 界面层（EditorScreen.kt）

- 编辑器模式：`BasicTextField` 只显示 `_diaryContent`（已无 frontmatter）
- 预览模式：`MdRenderer(text = diaryContent, ...)` → diaryContent 已经是过滤后的
- 编辑器与预览行为一致，都过滤

#### 1.3 桌面便签（QuickDailyWidget.kt）

- `updateWidget()` 中读取文件后，如果 `filterFrontmatter=true`，用相同规则 strip 掉 frontmatter 再 set 到 `widget_content`
- 由于 RemoteViews 不能读 SharedPreferences 需跑在主线程，在 IO 线程读取后传到主线程 strip

#### 1.4 快速添加/分享路径的 frontmatter 感知

**NoteEditActivity.appendToDiary()** 和 **MainActivity.saveSharedTextToDiary()**：
- 这两个路径目前直接读文件、操作、写回
- 需要升级：读取文件 → 解析出 frontmatter（如有）和 body → 在 body 中查找锚点、插入内容 → 重组 frontmatter + body → 写回
- 这是最安全的方案：无论设置是否开启，只要文件有 frontmatter 就保护它，不依赖配置状态

#### 1.5 设置界面（SettingsScreen.kt）

在「速记设置」区域新增一个 Switch：
```
[ 过滤 Frontmatter 显示 ] 开/关
副说明文字：开启后编辑器和桌面便签将隐藏 YAML 抬头数据，保存时自动保留
```

---

## 需求二：图片快速添加

### 目标
两条输入路径，最终效果一致：图片被复制到 vault，日记中插入 Markdown 图片引用。

---

### 2A：Android 分享图片到 QD

#### 2A.1 Manifest 改动

为 `MainActivity` 增加两个 intent-filter：

```xml
<!-- 单图分享 -->
<intent-filter>
    <action android:name="android.intent.action.SEND" />
    <category android:name="android.intent.category.DEFAULT" />
    <data android:mimeType="image/*" />
</intent-filter>
<!-- 多图分享 -->
<intent-filter>
    <action android:name="android.intent.action.SEND_MULTIPLE" />
    <category android:name="android.intent.category.DEFAULT" />
    <data android:mimeType="image/*" />
</intent-filter>
```

#### 2A.2 Intent 处理（MainActivity.kt）

**`handleShareIntent()`** 升级：
- 先判断 `intent.type?.startsWith("image/")`
- 单图：从 `Intent.EXTRA_STREAM`（Uri）取图片
- 多图：从 `Intent.EXTRA_STREAM`（`ArrayList<Uri>`）遍历

处理流程（抽取为公共方法，NoteEditActivity 也可复用）：
1. 解析配置中的 `imageStoragePath`（图片存储目录，相对于 vault）和 `imageNamingFormat`
2. 对每张图片：`contentResolver.openInputStream(uri)` → 生成文件名 → 复制到 vault 的目标目录
3. 生成 Markdown 引用，按配置决定 `![](path)` 还是 `![描述](path)`
4. 调用日记写入逻辑（含 frontmatter 保护），在锚点位置插入所有图片引用

#### 2A.3 权限

图片分享进来的 URI 通常带有临时读取权限，`contentResolver.openInputStream()` 可以直接使用，无需额外权限声明。但复制到 vault 需要存储写入权限（已有 `MANAGE_EXTERNAL_STORAGE`）。

---

### 2B：悬浮窗内添加图片按钮

#### 2B.1 UI 改动（NoteEditActivity.kt）

悬浮窗布局调整，新增元素：

```
┌─────────────────────────────┐
│  取消                 保存  │
│                             │
│  ┌─────┐                    │
│  │ 📷  │  ← 新增图片按钮    │
│  └─────┘                    │
│                             │
│  ┌───┐ ┌───┐ ┌───┐         │
│  │ 🖼 │ │ 🖼 │ │ 🖼 │  ← 缩略图预览区  │
│  │  ✕│ │  ✕│ │  ✕│    每个右上角有 X  │
│  └───┘ └───┘ └───┘         │
│                             │
│  ┌───────────────────────┐  │
│  │ 写点什么...           │  │
│  │                       │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

#### 2B.2 图片选择

- 点击 📷 按钮 → 拉起 `ActivityResultContracts.GetMultipleContents()`（兼容旧版）或 `PickMultipleVisualMedia`（API 33+）
- MIME type：`"image/*"`
- 用户可一次选择多张

#### 2B.3 缩略图预览

- 选中后立即为每张图片生成缩略图（`ContentResolver.loadThumbnail()` 或自己缩略）
- 预览区横向滚动排列，每张缩略图右上角有一个小 ✕ 按钮
- 点击 ✕ → 从已选列表移除该图片，预览区刷新

#### 2B.4 保存逻辑

用户点击「保存」或按回车时：
1. 收集文本内容（按时间戳格式装配）
2. 遍历已选图片 → 复制到 vault 目录 → 生成 Markdown 引用
3. 图片引用与文本组合，按时间戳格式插入
4. 写入日记文件（含 frontmatter 保护）

**保存顺序约定**：
- 先文本后图片（文本按 timestampFormat 格式，图片引用追加在后面）

---

### 2C：Obsidian 附件配置读取

#### 2C.1 读取 `.obsidian/app.json`

在 `AppState` 新增方法：

```kotlin
suspend fun loadObsidianAppConfig(vaultPath: String): ObsidianAppConfig?
```

返回的数据类：
```kotlin
data class ObsidianAppConfig(
    val attachmentFolderPath: String = "/"
)
```

JSON 配置键：`attachmentFolderPath`

#### 2C.2 设置界面新增配置项

在「速记设置」区域下方新增两个配置：

**图片存储目录**（Image Storage Path）
- 文本框 + SAF 文件夹选择器按钮
- 读取 `.obsidian/app.json` 后自动填入 `attachmentFolderPath` 的值
- 为空时：图片保存到 **日记文件同目录**
- 不为空时：图片保存到 `vault/该路径/`

**图片文件名格式**（Image Naming Format）
- 下拉选择框，选项：
  | 值 | 示例 |
  |---|---|
  | `timestamp_ext`（默认） | `20260708_143021.jpg` |
  | `timestamp_original` | `20260708_143021_photo.jpg` |
  | `original` | `photo.jpg` |

**图片 Markdown 格式**（Image Link Format）
- 下拉选择框，二选一：
  | 值 | 示例 |
  |---|---|
  | `bare`（默认） | `![](assets/20260708_143021.jpg)` |
  | `described` | `![20260708_143021](assets/20260708_143021.jpg)` |

#### 2C.3 新配置项在 DiaryConfig 中的字段

```kotlin
data class DiaryConfig(
    // ... 现有字段
    val imageStoragePath: String = "",        // 为空则存到日记同目录
    val imageNamingFormat: String = "timestamp_ext",
    val imageLinkFormat: String = "bare"      // "bare" | "described"
)
```

#### 2C.4 「从 Obsidian 读取配置」按钮增强

在设置界面，现有的「从 Obsidian 读取配置」按钮目前只读 `daily-notes.json`。1.4 版本中：
- 同时读取 `.obsidian/daily-notes.json`（日记配置，已有）
- 同时读取 `.obsidian/app.json`（附件目录，新增）
- 一次性填入日记文件夹、日期格式、模板路径、图片存储路径

---

## 需求三：日记文件名中的 WW/ww 解析支持

### 目标
修复 `DateUtil.convertObsidianFormat()` 未处理周格式的问题，支持 `WW`。

### 改动范围

仅修改 `DateUtil.kt` 中的 `convertObsidianFormat()` 方法。

### 具体方案

在现有的替换链中插入 `WW` → `ww` 映射：

```kotlin
fun convertObsidianFormat(format: String): String {
    return format
        .replace("dddd", "EEEE")
        .replace("ddd", "EEE")
        .replace("YYYY", "yyyy")
        .replace("WW", "ww")    // ← 新增：Obsidian ISO 周数 → Java week-of-year
        .replace("DD", "dd")
        .replace("YY", "yy")
}
```

**替换顺序说明**：
- `WW` 放在 `DD` 之前，因为 `WW` 和 `DD` 无字符冲突，但逻辑上周格式先于日格式处理更清晰
- 不处理单数 `w`（Obsidian 用 `w` 表示本地周数，Java 也是 `w`，行为基本一致）
- 不处理单数大写 `W`（Obsidian 的 `W` = ISO 周数不补零，Java 的 `W` = week-of-month，两者语义冲突，不做映射）

### 影响范围

所有使用 `DateUtil.todayStr()` 的地方：
- `AppState.todayFilePath()` — 生成今日日记路径
- `NoteEditActivity.appendToDiary()` — 生成日记路径
- `MainActivity.saveSharedTextToDiary()` — 生成日记路径
- `QuickDailyWidget.updateWidget()` — 生成日记路径

全部自动受益，无需单独修改。

---

## 配置数据汇总

### DiaryConfig 新增字段

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `filterFrontmatter` | `Boolean` | `false` | 是否过滤 frontmatter 显示 |
| `imageStoragePath` | `String` | `""` | 图片存储相对路径（空=日记同目录） |
| `imageNamingFormat` | `String` | `"timestamp_ext"` | 图片文件名规则 |
| `imageLinkFormat` | `String` | `"bare"` | 图片 Markdown 链接格式 |

### 新数据类

```kotlin
data class ObsidianAppConfig(
    val attachmentFolderPath: String = "/"
)
```

### SharedPreferences 新增键

| 键 | 对应字段 |
|---|---|
| `filter_frontmatter` | `filterFrontmatter` |
| `image_storage_path` | `imageStoragePath` |
| `image_naming_format` | `imageNamingFormat` |
| `image_link_format` | `imageLinkFormat` |

---

## 不涉及变动的范围

- `MdRenderer.kt` — 不需要改，frontmatter 过滤在 editor 层已处理好，预览直接消费过滤后的字符串
- `QuickTileService.kt` — 磁贴作用只是启动 NoteEditActivity，所有改动在 Activity 内部
- `QuickShortcutActivity.kt` — 同理
- `QuickAccessibilityService.kt` — 不需要动
- `UriUtil.kt` — 不需要动
- `Debounce.kt` — 不需要动
- `FileUtil.kt` — 不需要动
- `Theme.kt` — 不需要动
- `build.gradle.kts` — 不需要新增依赖
